/// This enum defines the different types of fields supported
enum FieldType {
  text,
  numeric,
  numericShorthand,
  quantity,
  percentage,
  amount,
  amountShorthand,
  date,
  dateRange,
  toggle, // On/Off
  widget,
}
